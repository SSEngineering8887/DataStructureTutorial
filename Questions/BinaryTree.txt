In computer science, a binary tree is a tree data structure in which each node has at most ____, which are referred to as the left child and the right child
~
In a normal binary search tree the ______ of nodes depends almost entirely on the order in which they were added.
~
To actually define a binary tree in general, we must allow for the possibility that only _____ of the children may be empty. 
~
An empty set is an extended binary tree  if T1 and T2 are _________ 
~
A ______ naturally imparts a notion of levels (distance from the root), thus for every node a notion of children may be defined as the nodes connected to it a level below.
~





